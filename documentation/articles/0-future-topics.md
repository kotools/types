# Future topics

A living backlog of candidate topics for upcoming community articles. Pick
the next topic from here when starting a new `<n>-<slug>` article (see
`README.md` for the naming convention and distribution checklist).

Each entry lists the source area, a one-line hook, and a readiness note. Add
new ideas at the bottom; remove an entry once it becomes a published article.

| # | Topic                                                              | Source area                                                                                                                            | Hook                                                                                                                                | Status                                                   |
|---|--------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------|
| 1 | Non-empty collections without null checks                          | `kotools.types.collection` — `NotEmptyList`/`Set`/`Map`                                                                                | `head` is guaranteed to exist, `tail` may be empty — no `Optional`/`null` dance to read the first element.                          | Ready (stable)                                           |
| 2 | Bounded integers as compile-time constraints                       | `kotools.types.number` — `AnyInt`, `PositiveInt`, `NegativeInt`, `NonZeroInt`, `StrictlyPositiveInt`, `StrictlyNegativeInt`, `ZeroInt` | A sealed-interface hierarchy that makes "this Int can never be negative" a type, not a runtime check — and arithmetic preserves it. | Ready (stable)                                           |
| 3 | Result-based conversions instead of implicit exceptions            | `org.kotools.types` — `toXxx(): Result<T>`, `orThrow()`, `resultOf { }` (`ResultContext`)                                              | Validation failures are values you handle, not exceptions that surprise you three calls later.                                      | Ready (stable)                                           |
| 4 | Validating email addresses without a giant regex                   | `org.kotools.types` — `EmailAddress`, `EmailAddressRegex` presets (`default`/`alphabetic`/`alphanumeric`, since 5.1.0)                 | Most "email regex" snippets online are either too strict or famously wrong — here's what shipping presets look like instead.        | Ready (stable)                                           |
| 5 | Keeping invariants across serialization boundaries                 | `subprojects/kotlinx-serialization` — contextual serializers for `EmailAddress`/`EmailAddressRegex`                                    | Deserializing untrusted JSON shouldn't let invalid data sneak past your domain types — these serializers re-validate on the way in. | Ready (experimental module)                              |
| 6 | How a type goes from experimental to stable (and how it's retired) | `documentation/declarations-lifecycle.md` — `@ExperimentalKotoolsTypesApi` lifecycle (WARNING → ERROR → HIDDEN → removal)              | A look at the staged process that lets the library evolve its API without breaking users overnight.                                 | Ready (process/meta topic)                               |
