---
description: "Phase 3 – implement the plan exactly as specified"
---

Implement exactly the plan. Follow the commit breakdown from the plan.

For every public API change, ship tests, KDoc samples, and the refreshed ABI
dump in the same commit. Commit each unit of change using the gitmoji format:
`<emoji> <message> (<#issue>)`.

$ARGUMENTS
